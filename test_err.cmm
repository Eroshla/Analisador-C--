// Teste com erros de sintaxe
// Este arquivo contém vários erros propositais

int main() {
    // Erro 1: falta ponto e vírgula
    int x
    
    // Erro 2: operador inválido
    x =@ 10;
    
    // Erro 3: parênteses não fechados
    if (x > 0 {
        x = x + 1;
    }
    
    // Erro 4: chaves não fechadas
    while (x < 10) {
        x = x + 1;
    
    // Erro 5: token inválido
    x = 123abc;
    
    // Erro 6: string não fechada
    char *msg = "hello world;
    
    // Erro 7: caractere não fechado
    char c = 'a;
    
    // Erro 8: símbolo não reconhecido
    x = x $ 5;
    
    // Erro 9: estrutura de for inválida
    for (int i = 0 i < 10 i++) {
        x = x + 1;
    }
    
    return 0;
